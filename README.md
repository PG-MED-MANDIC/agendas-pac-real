agendas-pac-real

Dashboard Triagem — SLMandic (`index.html`, estático, sem build). Dados vêm da API da
ConsultaJá; ver `pipeline/README.md` para como atualizar (`python pipeline/atualizar_tudo.py`
ou duplo clique em `Atualizar Dashboard.bat`). Commit/push continuam manuais de propósito.
